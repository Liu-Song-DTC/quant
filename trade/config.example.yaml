# 实盘交易配置
# 复制为 config.yaml 并填入你的真实信息

# start_date: 实盘开始日期, 用于自动计算调仓日(每20个交易日)
start_date: "2026-04-30"

# 微信推送 (Server酱 sct.ftqq.com)
notification:
  enabled: false
  sckey: "YOUR_SCKEY"

# 代理配置 (akshare_proxy_patch)
proxy:
  host: ""
  auth_token: ""
  retry: 30
