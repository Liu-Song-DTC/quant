# 实盘交易配置（独立于回测策略配置）
portfolio:
  init_cash: 100000
  max_position: 10

# 微信推送（Server酱 sct.ftqq.com）
notification:
  enabled: false
  sckey: ""

# 文件路径
state_file: portfolio_state.json
report_dir: reports/
